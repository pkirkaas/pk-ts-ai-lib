[[reactemotion]]

{| Emotion & MUI |}

Emotion can create style objects from strings and objects. It can convert these style objects to CSS class names and inject them into the DOM.

I want to be able to debug and analyze the style objects and their CSS class names.

Various `@emotion` utilities/functions accept style objects and strings, create and inject a css class into the DOM, and return a generated css class name as a string.

If I have a  css class name generated by emotion from style object/strings, how can I find/see/return the actual  CSS style object represented by the class names?

I have a CLI Node application that I use to test my various React components, so I can just console.log a style object and see the CSS, if I can get it from the class names.