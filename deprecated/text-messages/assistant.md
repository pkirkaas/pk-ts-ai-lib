[[code]]

I am a software developer. I use `VSCode` as my IDE. I have written several CLI AI ChatBots using several different AI APIs and LLM models. I also run several small LLMs on my local development machine with `LMStudio` and `Ollama`, so I am  familiar with the LLM ecosystem and comfortable writing AI code in Python and JavaScript.

I am also working on several large, complex `TypeScript` projects, and I am looking for an AI assistant to help me. There are many AI coding assistants available, I have tried many, but I haven't found one that meets my requirements, which are:

- I am NOT interested in code completion.
- I REQUIRE an assistant that can examine and understand all the code in my project as context. It should be able to answer questions about the code, suggest changes, and in particular understand build/configuration problems.

- That implies it should be configurable to specify which files and directories to include in the context and which to exclude.

- The key requirement is to be able to specify the context of the project, and to be able to specify which files and directories to include in the context and which to exclude. 

- It should be free or open source - except for whatever costs I incur for API calls.

- It should allow me to configure it to use different LLM Endpoints & API keys - cloud-based or self-hosted.

- Since this is a rapidly changing landscape, please double check and ensure the solutions you suggest are current and up to date as of Fall 2024.

It can be a VSCode extension, or a standalone application, or a python/javascript library/package available on GitHub I can clone and configure to run in a terminal on my machine.

I don't care if it is a VSCode extension or a CLI/Terminal application - my primary use case is a chat agent with knowledge & understanding of my current codebase I can ask about specific issues with my code, configuration, etc.

Please suggest several options and explain the pros and cons of each.