[[ts]]

I am using TypeScript to compile `*.ts` & `*.tsx` files to `*.js` files and put them in a `dist` folder.

This works, but some of my `*.tsx` files import local `*.module.css` files - but the `*.module.css` files are not compiled to `*.js` files or copied to the `dist` folder. Can you advise how to get the `*.module.css` files compiled and copied to the `dist` folder?

[[fetsconfig]]

{|__ASK__|}