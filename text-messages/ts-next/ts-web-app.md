[[webapp]] The backend API and React front end are already implemented, but without authentication.

The initial task is to implement registration/authentication/login for multiple providers (direct email/password, google, facebook, twitter, github, etc.).

Your task is not to instruct on how to implement a node api or react front end - your task is only to provide detailed instructions on how to implement a complete, thorough, secure authentication parts for both front end and API, using any appropriate NPM libraries and packages.

Provide detailed instructions with code examples and explanations.

The proposed approach is to use `passport.js` and `jwt` tokens for authentication, but you can use any other libraries and packages.



