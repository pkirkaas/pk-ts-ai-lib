# AIDER GUIDELINES

- This is a TypeScript project
- All the source files are in the `src` subdirectory. All references to `.ts` files should be considered relative to `src`
- The compiled TypeScript files are in the `dist` directory. Pay no attention to these.
